
  <p>name: <%= user.name %></p>
  <p>email: <%= user.email %></p>
  <p><%= gravatar_for(user, size: 100)%></p>
  <% if current_user.admin? && !current_user?(user)  %>
    <%= link_to "delete", user, method: :delete, confirm: "Are you sure?" %>
  <% end %>
